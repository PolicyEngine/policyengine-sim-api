[project]
name = "policyengine-stage12-persistence"
version = "0.1.0"
readme = "README.md"
authors = [
    {name = "PolicyEngine", email = "hello@policyengine.org"},
]
license = {file = "../../LICENSE"}
requires-python = ">=3.13,<3.14"
dependencies = [
    "policyengine-simulation-contract",
    "psycopg[binary]>=3.2,<4",
    "sqlalchemy>=2,<3",
]

[project.optional-dependencies]
test = [
    "pytest>=8.3.4",
    "pytest-cov>=6.1.1",
]
build = [
    "black>=25.1.0",
    "pyright>=1.1.401",
]

[build-system]
requires = ["hatchling>=1.26"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
sources = ["src"]
only-include = ["src/policyengine_stage12_persistence"]
packages = ["src/policyengine_stage12_persistence"]

[tool.uv.sources]
policyengine-simulation-contract = { path = "../policyengine-simulation-contract", editable = true }

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
